<h1><%= Patient.human_attribute_name(:addiction) %>日記の登録</h1>
<%= form_with(model: @diary, url: patient_addicdatum_diaries_path(@addicdatum)) do |form| %>
<%= render "form", diary: @diary,form:form %>
<% end %>

<br>

<div>
  <%= link_to I18n.t("back"), patient_addicdatum_diaries_path,class: 'btn btn-primary' %>
</div>
